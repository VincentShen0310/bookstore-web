<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="main.java.dao.BookDAO">

	<resultMap type="Book" id="BookResult">
		<id column="b_id" jdbcType="INTEGER" property="id" />
		<result column="b_name" jdbcType="VARCHAR" property="name" />
		<result column="b_description" jdbcType="VARCHAR" property="description" />
		<result column="b_status" jdbcType="INTEGER" property="status" />
		<result column="b_is_display" jdbcType="INTEGER" property="isDisplay" />
		<result column="b_author_id" jdbcType="INTEGER" property="authorId" />
		<collection property="author" resultMap="main.java.dao.AuthorDAO.AuthorResult"/>
	</resultMap>


	<select id="queryBookByID" parameterType="int" resultMap="BookResult">
		SELECT
		b.id b_id,b.name b_name,b.description b_description,
		b.status b_status,b.is_display b_is_display,
		b.author_id,a.name,a.description
		FROM book b
		left join author a on b.author_id=a.id
		<where>
			b.id=#{id}
		</where>
	</select>
	
	<select id="queryBookByAuthor" resultMap="BookResult">
		SELECT
		b.id b_id,b.name b_name,b.description b_description,
		b.status b_status,b.is_display b_is_display,
		b.author_id,a.name,a.description
		FROM book b 
		left join author a on b.author_id=a.id 
		WHERE b.author_id =#{id} 
		LIMIT 2
	</select>

	<select id="queryAllBooks" resultType="Book" resultMap="BookResult">
		SELECT
		id,name,description,status,is_display,author_id,update_time,update_user FROM book
	</select>
	
	<select id="queryBooksByCondition" resultMap="BookResult">
		SELECT
		b.id b_id,b.name b_name,b.description b_description,
		b.status b_status,b.is_display b_is_display,
		b.author_id,a.name
		FROM book b
		left join author a on b.author_id=a.id
		<where>
			b.status=1 AND b.is_display=1
		<if test="name != null and name !='' ">
			AND b.name like "%"#{name}"%"
		</if>
		<if test="description != null and description !='' ">
			AND b.description like "%"#{description}"%"
		</if>
		<if test="status != null and status !='' ">
			AND b.status = #{status}
		</if>
		<if test="isDisplay != null and isDisplay !='' ">
			AND b.is_display = #{isDisplay}
		</if>
		<if test="authorName != null and authorName !='' ">
			AND a.name like "%"#{authorName}"%"
		</if>
		</where>
	</select>
	
</mapper>
